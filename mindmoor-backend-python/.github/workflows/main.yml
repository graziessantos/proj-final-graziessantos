name: Teste de Conexao
on: [push]

jobs:
  verificar-arquivos:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar o repo
        uses: actions/checkout@v4

      - name: Mostrar arquivos
        run: ls -la